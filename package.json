{
  "name": "open-gov-climate",
  "type": "module",
  "version": "2.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "npx pagefind --site dist && npm run verify",
    "verify": "node scripts/verify-publication.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.2.4",
    "@astrojs/preact": "^6.0.4",
    "preact": "^10.29.8",
    "maplibre-gl": "^5.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
